#include <stdio.h>
int main() 
{
    int i,x,rev=0;
    printf("\n enter number to check ");
    scanf("%d",&i);
    x=i;
    while(i>0)
    {
        rev=(rev*10)+i%10;
        i=i/10;
    }
    if (rev==x)
    printf("pallindrome");
    else
    printf(" not pallindrome");
     return 0;
}
